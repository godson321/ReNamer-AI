<UserControl x:Class="ReNamer.Controls.NumericUpDown"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="23" MinWidth="50">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="17"/>
        </Grid.ColumnDefinitions>
        <TextBox x:Name="PART_TextBox" Grid.Column="0" VerticalContentAlignment="Center"
                 FontSize="12" Padding="2,0" BorderThickness="1,1,0,1"
                 PreviewTextInput="TextBox_PreviewTextInput"
                 TextChanged="TextBox_TextChanged"
                 LostFocus="TextBox_LostFocus"/>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <RepeatButton x:Name="PART_UpButton" Grid.Row="0" FontSize="7" Content="▲"
                          Focusable="False" Delay="500" Interval="50"
                          Click="UpButton_Click" Padding="0" BorderThickness="1,1,1,0"/>
            <RepeatButton x:Name="PART_DownButton" Grid.Row="1" FontSize="7" Content="▼"
                          Focusable="False" Delay="500" Interval="50"
                          Click="DownButton_Click" Padding="0" BorderThickness="1,0,1,1"/>
        </Grid>
    </Grid>
</UserControl>
